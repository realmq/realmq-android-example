pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven {
            url  'https://repo.eclipse.org/content/repositories/paho-releases/'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url  'https://repo.eclipse.org/content/repositories/paho-releases/'
        }
    }
}
rootProject.name = "rmq test"
include ':app'
